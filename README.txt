EJERCICIO CREAR USUARIO

Ejercicio para crear un usuario. Se guarda un archivo .txt o json (a legir por consola). 
He guardado y recogido la variable del tipo de archivo desde el archivo app.config. 
También he guardado la variable de la ruta donde se guardará el archivo en app.config.

